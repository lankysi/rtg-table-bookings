<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Welcome, <%- user.username || 'User' %>!</h1>
        <div class="profile-header">
            <% if (user.discord_id && user.avatar) { %>
                <img src="https://cdn.discordapp.com/avatars/<%- user.discord_id %>/<%- user.avatar %>.png" alt="User Avatar" class="profile-avatar">
            <% } else { %>
                <img src="/default-avatar.png" alt="Default Avatar" class="profile-avatar">
            <% } %>
            <p>Your ID: <%- user.id %></p>
        </div>
        <hr>
        <div class="profile-links">
            <h3>Quick Links</h3>
            <ul>
                <li><a href="/bookings" class="button primary-button">Book a Table</a></li>
                <% if (user.is_admin) { %>
                <h4>Admin controls</h4>
                <li><a href="/all_bookings" class="button primary-button">View all bookings</a></li>
                <li><a href="/manage_games" class="button primary-button">Manage games</a></li>
                <li><a href="/manage_tables" class="button primary-button">Manage tables</a></li>
                <% } %>
            </ul>
        </div>
        <div class="logout-link">
            <a href="/logout" class="button tertiary-button">Log Out</a>
        </div>
    </div>
</body>
</html>